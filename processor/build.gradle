apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    compile project(path: ':processor-lib')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
